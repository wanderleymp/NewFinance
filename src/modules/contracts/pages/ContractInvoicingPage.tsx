import React from 'react';

// Componente placeholder para ContractInvoicingPage
// Este componente foi criado para resolver erros de build
// Implemente a funcionalidade completa quando necessário

const ContractInvoicingPage: React.FC = () => {
  return (
    <div style={{ padding: 32, textAlign: 'center' }}>
      <h2>Faturamento de Contratos</h2>
      <p>Página temporariamente desativada para build sem dependências externas.</p>
    </div>
  );
};

export default ContractInvoicingPage;
